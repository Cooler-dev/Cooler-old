name: Build Cooler docs

on:
  push:
    paths: 
      - 'docs_src'
    

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Install mkdocs
        run: pip install mkdocs
      -
        name: Build
        uses: mkdocs build